server:
  port: 9002

spring:
  application:
    name: doctor-service

  data:
    redis:
      host: localhost
      port: 6379
      password: 1346798520
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1346798520
    url: jdbc:mysql://localhost:3306/doctor-service

  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
rocketmq:
  name-server: localhost:9876
  producer:
    group: "hospital-doctor-group"
mybatis:
  mapper-locations: classpath*:mapper/*.xml
