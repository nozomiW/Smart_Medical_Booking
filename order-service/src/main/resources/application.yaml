server:
  port: 9004


spring:
  application:
    name: order-service
  data:
    redis:
      host: localhost
      port: 6379
      password: 1346798520
      lettuce:
        pool:
          max-active: 32
          max-wait: 5000ms
          max-idle: 16
          min-idle: 8
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1346798520
    url: jdbc:mysql://localhost:3306/order_service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

rocketmq:
  name-server: localhost:9876
  producer:
    group: "hospital-order-group"

mybatis:
  mapper-locations: classpath*:mapper/*.xml

# 日志配置
logging:
  level:
    root: INFO
    com.cly.orderservice: DEBUG
    com.cly.orderservice.aspect: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
